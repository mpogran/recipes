%header
  .col-container
    #logo
      = link_to dashboard_path do
        = image_tag 'icon.svg'
    #navigation
    #controls
      = link_to 'sign up', new_user_path, id: 'signup', class: 'pill dark-blue'
      - if current_user
        = link_to 'log out', logout_path
      - else
        = link_to 'log in', login_path
